# Known Issues and Development Priorities

現在確認されている制約と、今後の対応方針を記録します。完了した試行錯誤の履歴ではなく、
利用・保守・次回開発時に判断が必要な項目だけを残します。

## 高優先度: ログ共有時のプライバシー

状態: アプリ内表示・コピーは対応済み（生ログは要注意）

アプリログには、実際のBluetooth端末名、完全なデバイスID、実行ファイルやデータフォルダーの
パスが含まれる場合があります。第三者へログを共有すると、利用端末やユーザー環境を推測できる
情報が意図せず含まれる可能性があります。

現在の対応:

- アプリ内の直近30行では、端末名を `端末A`、`端末B`、BluetoothデバイスIDを
  対応する匿名ID、Windowsユーザーフォルダーを `%USERPROFILE%` / `%APPDATA%`、
  実行ファイルのフルパスを `<APP_EXECUTABLE>` へ置換する。
- クリップボードには、画面に表示した匿名化済みログだけをコピーする。
- 同じ端末の名前とIDには同じ匿名ラベルを割り当て、調査に必要な時系列を維持する。
- 匿名化処理で端末名、完全なID、ユーザー名が残らないことを自動テストする。

残る注意点:

- ローカル調査用の `log.txt` と `log.previous.txt` は匿名化せず保存するため、端末名、
  完全なBluetoothデバイスID、ユーザーフォルダーが含まれる場合がある。
- 生ログをそのまま第三者、公開Issue、チャット、配布物へ共有しない。
- 外部へ共有するときは、原則としてアプリの「匿名化済みログをコピー」を使用する。

## 既知の制約: 外部切断検出と自動再接続

状態: 自動再接続機能を削除（意図的な仕様）

`AudioPlaybackConnection::State` は、一方の端末を手動切断した直後、もう一方の音声が
再生され続けていても `Closed` を返す場合があります。この値を切断判定に使用すると、
正常な接続を管理対象から外し、表示の誤更新や不要な再接続を起こします。

現在の方針:

- `AudioPlaybackConnection::State` を定期監視しない。
- 外部切断後の自動再接続をフロントエンドと保存設定で提供しない。
- スマートフォン側やBluetooth側から切断した後は、アプリで対象端末の「切断」を押して
  管理状態を解除してから、改めて「接続」を押す。
- 個別接続・個別切断の安定性を優先し、信頼できない検出方法を追加しない。

設定にある起動時の自動接続は、アプリ起動時だけ動作する別機能として維持します。

再検討する条件:

- 音声が実際に継続している接続を切断扱いしない、別のWindows APIまたはイベントが
  実機で確認できた場合。
- 最小検証版で、2端末の両接続順・両切断順に影響しないことを確認できる場合。
- 誤判定時に自動で接続を閉じたり作り直したりせず、安全に無視できる設計である場合。

この制約は、接続管理方式
`tauri-in-process-device-map-no-monitor-v12` の意図的な仕様です。
