{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for the Bluetooth receiver UI.",
  "windows": ["main", "quick"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen",
    "notification:default"
  ]
}
